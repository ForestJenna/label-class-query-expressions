# label-class-query-expressions
reads all data layers in a specified map inside an .aprx exports all label classes and label expressions (including multiple queries per layer) to one CSV includes each query and expression title/name and whether each query and expression is active.
